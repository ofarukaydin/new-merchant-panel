export enum orderStatus {
  ORDERED = 'Yeni',
  READY = 'Hazır',
  SHIPPING = 'Taşımada',
  DELIVERED = 'Teslim edildi',
  CANCELLED = 'İptal',
  REFUNDED = 'İade',
}

export enum orderDir {
  ascend = 'asc',
  descend = 'desc',
}
